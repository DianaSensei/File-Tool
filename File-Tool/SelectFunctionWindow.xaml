<Window x:Class="File_Tool.SelectFunctionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
        xmlns:local="clr-namespace:File_Tool"
        mc:Ignorable="d"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded"
        Title="Function" FontWeight="DemiBold" Height="350" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="250"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Name="_stackpanelParent">
            <TextBlock VerticalAlignment="Center" Margin="50,0" HorizontalAlignment="Stretch" FontWeight="DemiBold" FontSize="20" Text="Select Function"/>
            <ComboBox Name="_comboboxSelect" Loaded="ComboboxSelect_Loaded" 
                      materialDesign:ComboBoxAssist.ShowSelectedItem="True"
                      Margin="50,20" Height="35" SelectionChanged="ComboBox_SelectionChanged">
                <ComboBoxItem Height="35">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="FindReplace" Foreground="ForestGreen" Width="24" Height="24" VerticalAlignment="Center"/>
                        <Label Content="Replace" Height="35" FontSize="15" FontWeight="DemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                </ComboBoxItem>
                <ComboBoxItem Height="35">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="FormatUppercase" Width="24" Height="24" VerticalAlignment="Center"/>
                        <Label Content="NewCase" Height="35" FontSize="15" FontWeight="DemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                </ComboBoxItem>
                <ComboBoxItem Height="35">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="KeyRemove" Width="24" Height="24" VerticalAlignment="Center"/>
                        <Label Content="Move" Height="35" FontSize="15" FontWeight="DemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                </ComboBoxItem>
                <ComboBoxItem Height="35">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Ornament" Width="24" Height="24" VerticalAlignment="Center"/>
                        <Label Content="Fullname Normalize"  Height="35" FontSize="15" FontWeight="DemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                </ComboBoxItem>
                <ComboBoxItem Height="35">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="OneUp" Width="24" Height="24" VerticalAlignment="Center"/>
                        <Label Content="Unique Name" Height="35" FontSize="15" FontWeight="DemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                </ComboBoxItem>
            </ComboBox>
            <StackPanel Name="_stackpanelParameterReplace">
                <TextBox Style="{StaticResource MaterialDesignFloatingHintTextBox}" Text="{Binding Needle}" 
                         materialDesign:HintAssist.Hint="Find What"
                         HorizontalContentAlignment="Center" Margin="50,15,50,0"/>
                <TextBox Style="{StaticResource MaterialDesignFloatingHintTextBox}" Text="{Binding Hammer}" 
                         materialDesign:HintAssist.Hint="Replace By"
                         HorizontalContentAlignment="Center" Margin="50,15,50,0"/>
            </StackPanel>
            <StackPanel Name="_stackpanelParameterNewCase">
                <ComboBox Margin="50,15,50,0" Height="35" SelectedIndex="{Binding Case}" 
                          materialDesign:ComboBoxAssist.ShowSelectedItem="True"
                          materialDesign:HintAssist.Hint="Type">
                    <ComboBoxItem  Height="35">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="FormatUppercase" Width="24" Height="24" VerticalAlignment="Center"/>
                            <Label Content="To UpperCase" Height="35" FontSize="15" FontWeight="DemiBold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ComboBoxItem>
                    <ComboBoxItem  Height="35">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="FormatLowercase" Width="24" Height="24" VerticalAlignment="Center"/>
                            <Label Content="To LowerCase" Height="35" FontSize="15" FontWeight="DemiBold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ComboBoxItem>
                    <ComboBoxItem  Height="35">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="FormatLetterCaseUpper" Width="24" Height="24" VerticalAlignment="Center"/>
                            <Label Content="To LetterCase Upper" Height="35" FontSize="15" FontWeight="DemiBold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ComboBoxItem>
                </ComboBox>
            </StackPanel>
            <StackPanel Name="_stackpanelParameterMove">
                <TextBox Style="{StaticResource MaterialDesignFloatingHintTextBox}" Text="{Binding StartIndex}" materialDesign:HintAssist.Hint="Start Index" HorizontalContentAlignment="Center" Margin="50,15,50,0"/>
                <TextBox Style="{StaticResource MaterialDesignFloatingHintTextBox}" Text="{Binding Length}" materialDesign:HintAssist.Hint="Length" HorizontalContentAlignment="Center" Margin="50,15,50,0"/>
            </StackPanel>
        </StackPanel>
        <StackPanel Orientation="Horizontal" FlowDirection="RightToLeft" Grid.Row="1">
            <Button Content="Cancel" Grid.Row="1" Width="80" Click="BtnCancelFunction_Click" Margin="30,0,30,0"/>
            <Button Name ="_btnFunction" Content="Add" Grid.Row="1" Width="80" Click="BtnFunction_Click"/>
        </StackPanel>
        <!--materialDesign:Flipper Style="{StaticResource MaterialDesignCardFlipper}">
            <materialDesign:Flipper.FrontContent>
                <Grid Height="256" Width="200">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="160" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <materialDesign:ColorZone Mode="PrimaryLight" VerticalAlignment="Stretch">
                        <materialDesign:PackIcon Kind="AccountCircle" Height="128" Width="128"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Center" />
                    </materialDesign:ColorZone>
                    <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock>
                            James Willock
                        </TextBlock>
                        <Button Style="{StaticResource MaterialDesignFlatButton}" Command="{x:Static materialDesign:Flipper.FlipCommand}" Margin="0 4 0 0">
                            EDIT
                        </Button>
                    </StackPanel>
                </Grid>
            </materialDesign:Flipper.FrontContent>
            <materialDesign:Flipper.BackContent>
                <Grid Height="256" Width="200">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <materialDesign:ColorZone  Mode="Accent" Padding="6">
                        <StackPanel Orientation="Horizontal">
                            <Button Style="{StaticResource MaterialDesignToolForegroundButton}" Command="{x:Static materialDesign:Flipper.FlipCommand}"
                                    HorizontalAlignment="Left">
                                <materialDesign:PackIcon Kind="ArrowLeft" HorizontalAlignment="Right" />
                            </Button>
                            <TextBlock Margin="8 0 0 0" VerticalAlignment="Center">
                                       EDIT USER
                            </TextBlock>
                        </StackPanel>
                    </materialDesign:ColorZone>
                    <Grid  Grid.Row="1" Margin="0 6 0 0" HorizontalAlignment="Center"  VerticalAlignment="Top"  Width="172">
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <TextBox materialDesign:HintAssist.Hint="First name" materialDesign:HintAssist.IsFloating="True" Margin="0 12 0 0">
                            James
                        </TextBox>
                        <TextBox Grid.Row="1" materialDesign:HintAssist.Hint="Last name" materialDesign:HintAssist.IsFloating="True" Margin="0 12 0 0">
                            Willock
                        </TextBox>
                        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0 12 0 0" HorizontalAlignment="Right">
                            <TextBlock VerticalAlignment="Center">
                                Email Contact
                            </TextBlock>
                            <ToggleButton Margin="8 0 0 0"></ToggleButton>
                        </StackPanel>
                        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0 12 0 0" HorizontalAlignment="Right">
                            <TextBlock VerticalAlignment="Center">
                                Telephone Contact
                            </TextBlock>
                            <ToggleButton Margin="8 0 0 0"></ToggleButton>
                        </StackPanel>
                    </Grid>
                </Grid>
            </materialDesign:Flipper.BackContent>
        </-->
    </Grid>
</Window>
